import 'zone.js';
import 'zone.js/testing';

// The Angular testing environment is initialized per spec file (see the `beforeAll`
// hooks in the *.spec.ts files). Initializing it here as well makes those calls throw
// "Cannot set base providers because it has already been called".
